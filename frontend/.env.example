# Backend API base URL (no trailing slash). Defaults to http://localhost:5090 if unset.
VITE_API_URL=http://localhost:5090

# Use real GET /api/snippets. If unset or false, the app uses a large in-memory mock feed (for UI testing).
# VITE_USE_SNIPPET_API=true
