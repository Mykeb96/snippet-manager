# Backend API base URL (no trailing slash). Defaults to http://localhost:5090 if unset.
VITE_API_URL=http://localhost:5090

# Set to true to use an in-memory mock feed (no backend). Omit or set false to use the real API so posts persist.
# VITE_USE_MOCK_FEED=true
